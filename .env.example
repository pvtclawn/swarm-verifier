# Environment Variables

# Path to secrets vault (required for wallet operations)
VAULT_PATH=/path/to/.vault/secrets.json

# Optional: Foundry path if not in ~/.foundry/bin
# FOUNDRY_PATH=/path/to/foundry/bin
